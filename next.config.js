/** @type {import('next').NextConfig} */
const nextConfig = {
  // Don't use 'export' output for Vercel deployment
  // This was causing the routes-manifest.json to not be generated
};

module.exports = nextConfig; 